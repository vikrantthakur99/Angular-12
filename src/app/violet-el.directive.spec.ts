import { VioletElDirective } from './violet-el.directive';

describe('VioletElDirective', () => {
  it('should create an instance', () => {
    const directive = new VioletElDirective();
    expect(directive).toBeTruthy();
  });
});
